# YAML file to locate raw data outputs from ONT or NOMADS tools

minknow:
  as_sudo: True
  with_symlink: False
  
nomadic:
  as_sudo: False
  with_symlink: True

savanna:
  as_sudo: False
  with_symlink: True
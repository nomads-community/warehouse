#This file defines key criteria for filtering the bcftools data
include_mut_type: ["missense","synonymous", "stop_gained", "*missense", "*synonymous", "*stop_gained",  ]
  # exclude: [".", "intron", "splice_region", "splice_acceptor"]

wsaf_threshold: 0.05 # 5% prevalence is probably conservative, but ensures no sequencing errors


